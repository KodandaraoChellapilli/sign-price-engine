services:
  - type: web
    name: sign-price-engine-4
    env: python

    plan: free

    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt

    startCommand: uvicorn app:app --app-dir backend --host 0.0.0.0 --port 10000

    runtime: python-3.11.8